This directory is used by CodeIgniter 4 for:
- writable/cache/       → View cache
- writable/logs/        → Error and debug logs
- writable/session/     → Session files
- writable/database.sqlite → SQLite database file (created by migrations)

These folders are auto-created by CI4 if writable permissions are set.
